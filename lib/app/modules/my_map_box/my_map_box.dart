import 'package:flutter/material.dart';

class MyMapBoxView extends StatelessWidget {
  const MyMapBoxView({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(
          child: Text('MyMapBoxView'),
        ),
      );
}
