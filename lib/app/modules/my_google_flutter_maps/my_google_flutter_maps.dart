import 'package:flutter/material.dart';

class MyGoogleFlutterMapsView extends StatelessWidget {
  const MyGoogleFlutterMapsView({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(
          child: Text('MyGoogleFlutterMapsView'),
        ),
      );
}
